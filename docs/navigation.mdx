---
sidebar_position: 8
---

# Navigation

Use `navigation{/*your navigation codes*/}` to do navigation operations. It is available only inside `WirelessViewModel` instance.

```kt
navigation {//use navHostController methods directly. navigate, popBackStack,...
    navigate(Routes.login.full)
}
```