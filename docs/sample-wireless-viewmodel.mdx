---
sidebar_position: 7
---

# Sample WirelessViewModel

```kotlin
class MyViewModel: WirelessViewModel(){
    override fun interCom(message: InterCom) {
        
    }

    override fun onBack() {
        
    }

    override fun eventBusDescription(): EventBusDescription? {
        return null
    }

    override fun onNotification(id: Any?, arg: Any?) {
        
    }

    override fun onStartUp(route: Route?, arguments: Bundle?) {
        
    }
}
```

Normally we extends `ViewModel` but in *J3* we have to extend `WirelessViewModel`.